# this are automatically configured after creating modles and registered...ll
from django.apps import AppConfig


class KeshavConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'keshav'

